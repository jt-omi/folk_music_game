# 四川民族音乐游戏网页发布说明

## 本地打开

不要直接双击 `sichuan-music-game.html`。游戏里有音频、视频和 JSON 配置，浏览器在 `file://` 模式下可能会拦截资源。

推荐方式：

1. 双击项目根目录的 `启动网页游戏.bat`
2. 电脑浏览器打开 `http://127.0.0.1:8765/`
3. 手机和电脑连接同一个 Wi-Fi 后，打开命令行里显示的局域网地址

## 正式发布到网页

把 `outputs` 文件夹里的全部内容上传到任意静态网页空间即可，例如学校服务器、Nginx、Apache、GitHub Pages、Netlify、Vercel 等。

网页入口文件是：

`outputs/index.html`

主游戏文件是：

`outputs/sichuan-music-game.html`

上传时必须保持 `outputs` 目录内的文件相对结构不变，尤其是：

- `assets/`
- `*.css`
- `*.js`
- `*.json`
- 音频和视频文件

## 手机端建议

游戏已经加入 `mobile-responsive.css` 做手机适配。地图和剧情可以竖屏浏览；节奏类、舞蹈类小游戏建议横屏体验，按钮和舞台会更完整。

## 作者工具

以下页面只给制作者使用，不会出现在学生菜单中：

- 强弱拍标注器：`outputs/strong-weak-author.html`

本地服务器启动后，可以打开：

`http://127.0.0.1:8765/strong-weak-author.html`
